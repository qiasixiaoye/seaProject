# 跨机器迁移指南

本文件随完整离线包一起分发,目标:在一台**全新 Windows 机器**上把"海洋 RAG + 多 Agent"系统跑起来。

迁移包文件:`ocean-rag-migration-YYYYMMDD.tar`(约 16GB,含源码 + 数据 + 12.4GB 离线镜像 + 1.2GB RAGFlow 知识库快照)。

---

## 0. 为什么是"整包拷贝"而不是 git clone

- 本会话的 UI / 「你好」短路 / PDF 导出改动**已提交**(`.git` 一并打进包,历史完整)。
- 但 `docker_deploy/` 的部署 compose 用 **volume 挂载**宿主机源码(`../backend`、`../geo_agent`、`../geo_api.py`、`../frontend`、`../data` 等),代码不是烤进镜像的。所以:
  - **镜像可以旧**(依赖没变),代码从宿主机实时读 → 拷过去的源码必须是最新的。
  - 必须连 `data/`、`instance/`、`geoserver_data/` 等挂载目录一起带走。

---

## 1. 新机器前置条件

- 安装 **Docker Desktop**,确认 `docker version` 正常返回。
- 磁盘预留 ≥ 40GB(解包 16GB + 镜像导入后展开)。
- Win10 1803+ / Win11 自带 `tar.exe`,可直接解包。

---

## 2. 解包

```powershell
# 解到目标位置,例如 D:\
tar -xf ocean-rag-migration-YYYYMMDD.tar -C D:\
cd "D:\海洋rag+多agent\docker_deploy"
```

> 路径含中文不影响解包和运行,但构建时需关 BuildKit(见第 6 步)。如条件允许,放到纯英文路径更省心。

---

## 3. 导入离线镜像

```powershell
powershell -ExecutionPolicy Bypass -File scripts\load-images.ps1
```

脚本自动选 `image-tars/` 里最新的 `*-full-*.tar` 导入。完成后 `docker images` 应能看到:
backend / geo-api / nginx / geoserver / elasticsearch / mysql / minio / valkey / ragflow。

---

## 4. 配置 `.env`(关键)

**密钥的值不用重新获取、也不会过期** —— 本包是整文件夹打包(不看 gitignore),原机的根目录 `.env`(含真实密钥)**已经在包里**,位于 `..\.env`(即 `docker_deploy` 的上一级)。你要做的不是"重填",而是把这些值**抄进 `docker_deploy\.env`**。

部署用的 `docker_deploy\docker-compose.yml` 读的是 `docker_deploy\.env`,这个文件原机没建过(只有 `.env.example`),所以新机要新建:

```powershell
cd docker_deploy
Copy-Item .env.example .env       # 先用模板打底,拿到端口/RAGFlow栈等完整默认值
# 再打开 docker_deploy\.env,把下面 3 个值从上一级 ..\.env 里照抄过来
```

> 为什么不直接 `Copy-Item ..\.env .env`?根 `.env` 只有这 3 类变量,缺 `APP_PORT`、各端口、RAGFlow 栈密码等默认值,直接覆盖会丢。所以"用 `.env.example` 打底 + 抄 3 个值"最稳。
>
> ⚠️ 这个 16GB 包含真实密钥,别公开传(公开网盘/外发),自己 U 盘或内网拷没问题。

要抄/确认的值:

| 变量 | 填什么 | 说明 |
|------|--------|------|
| `DEEPSEEK_API_KEY` | `sk-af66...`(原机值) | 留空则 LLM 走启发式兜底,仍能跑 |
| `RAGFLOW_API_KEY` | `ragflow-cpQC...`(原机值) | 留空则检索走本地知识库兜底 |
| `RAGFLOW_DATASET_IDS` | `ec14420a...,aca6ff0e...`(原机值) | **绑定快照数据,照原值填,别改** |
| `RAGFLOW_BASE_URL` | `http://ragflow-cpu:9380` | 用包内自带 RAGFlow 栈时用容器名;若连外部 RAGFlow 改 `http://host.docker.internal:9380` |

> ⚠️ `.env.example` 里 RAGFlow 的密码 / 库名 / 端口与导出数据完全一致,**不要改动**,改了恢复的数据会对不上。

---

## 5. 恢复 RAGFlow 知识库数据

```powershell
powershell -ExecutionPolicy Bypass -File scripts\restore-ragflow-data.ps1
```

把 `ragflow/volumes/*.tar.gz` 解压进 `ragflow/data/{elasticsearch,mysql,minio,redis}` 与 `ragflow/logs`(用临时容器解压以保留权限)。

> 只用本地知识库兜底、不需要 RAGFlow 时,可跳过本步和第 6 步的 `--profile ragflow`。

---

## 6. 启动

```powershell
# 中文路径先关 BuildKit(纯英文路径可跳过这两行)
$env:DOCKER_BUILDKIT='0'; $env:COMPOSE_DOCKER_CLI_BUILD='1'

# 主系统(镜像已导入,无需 --build)
docker compose up -d

# 完整 RAGFlow 栈
docker compose --profile ragflow up -d
```

---

## 7. 验证

| 入口 | 地址 |
|------|------|
| Web 前端 | <http://127.0.0.1:8000> |
| 后端健康 | <http://127.0.0.1:8000/api/health> |
| GeoAgent API | <http://127.0.0.1:5001/api/health> |
| GeoServer | <http://127.0.0.1:8000/geoserver/web/> |
| RAGFlow Web | <http://127.0.0.1:8088> |
| RAGFlow API | <http://127.0.0.1:9380> |

冒烟测试:打开前端 → 框选区域 → 提问"评估当前框选区域风险" → 右侧审计链应逐节点实时刷新;输入"你好"应短路返回引导语而非整篇报告;报告生成后点"导出PDF"应下载。

---

## 8. 常见问题

- **构建报 `x-docker-expose-session-sharedkey` 非 ASCII**:中文路径 + BuildKit 的 bug,先设 `$env:DOCKER_BUILDKIT='0'`(本包已导入镜像,正常不触发构建)。
- **改了后端代码不生效**:部署包是挂载模式,`docker compose restart backend geo-api` 即可;前端改完浏览器硬刷新(Ctrl+F5)。
- **恢复的知识库为空 / 检索不到**:确认 `.env` 的 RAGFlow 密码端口未改、`RAGFLOW_DATASET_IDS` 为原值、第 5 步恢复成功。
- **目标机能联网想省 12.4GB 镜像**:删 `image-tars/*.tar`,第 3 步换成 `docker compose --profile ragflow build` + `docker compose --profile ragflow pull`;RAGFlow 数据仍按第 5 步恢复。

---

## 9. 端口清单(被占用时改 `.env`)

| 端口 | 服务 |
|------|------|
| 8000 | Web 前端(nginx) |
| 5001 | GeoAgent API |
| 8081 | GeoServer |
| 8088 / 9380 | RAGFlow Web / API |
| 19000 / 19001 | MinIO / 控制台 |
| 11200 / 13306 / 16379 | ES / MySQL / Redis(对外映射) |
